<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <module name="TreeWalker">

        <!-- Разрешаем import * для Lombok и JPA -->
        <module name="AvoidStarImport">
            <property name="excludes" value="lombok.*, jakarta.persistence.*, java.util.*"/>
        </module>

        <!-- Убираем обязательный Javadoc -->
        <module name="JavadocVariable">
            <property name="severity" value="ignore"/>
        </module>
        <module name="MissingJavadocMethod">
            <property name="severity" value="ignore"/>
        </module>
        <module name="DesignForExtension">
            <property name="severity" value="ignore"/>
        </module>

        <!-- Длина строки — до 120 символов -->
        <module name="LineLength">
            <property name="max" value="120"/>
            <property name="severity" value="info"/>
        </module>

        <!-- Неиспользуемые импорты -->
        <module name="UnusedImports"/>

        <!-- final для параметров — игнорируем -->
        <module name="FinalParameters">
            <property name="severity" value="ignore"/>
        </module>

        <!-- Magic numbers — игнорируем часто используемые -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2, 3, 4, 5, 8, 10, 16, 32, 64, 128, 255, 1024"/>
            <property name="severity" value="info"/>
        </module>

        <!-- Обязательные фигурные скобки -->
        <module name="NeedBraces"/>

    </module>

</module>